---
phase: 08-type-cleanup
plan: 01
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - src/lib/types/feedback.ts
  - src/lib/utils/status.ts
  - src/lib/stores/feedback.svelte.ts
  - src/routes/api/sse/[sessionId]/+server.ts
  - src/routes/api/shutdown/+server.ts
  - src/lib/stores/plan.svelte.ts
  - src/lib/components/feedback/ApprovalBar.svelte
  - src/lib/components/plan/IdleTimer.svelte
autonomous: true
plane_items: ["PA-59", "PA-60"]
---

<objective>
## Goal
Use PhaseStatus type consistently instead of inline string unions, and consolidate magic numbers into named constants.

## Purpose
Eliminate type drift (5 inline redeclarations of `"pending" | "approved" | "needs-work"`) and make timing values discoverable via named constants.

## Output
- PhaseStatus imported from status.ts in all locations
- Magic numbers replaced with named constants
- All 160 tests pass, build clean
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Source Files
@src/lib/utils/status.ts
@src/lib/types/feedback.ts
@src/lib/stores/feedback.svelte.ts
@src/routes/api/sse/[sessionId]/+server.ts
@src/routes/api/shutdown/+server.ts
@src/lib/stores/plan.svelte.ts
@src/lib/components/feedback/ApprovalBar.svelte
@src/lib/components/plan/IdleTimer.svelte
</context>

<acceptance_criteria>

## AC-1: PhaseStatus type used consistently
```gherkin
Given the PhaseStatus type defined in status.ts
When feedback.ts and feedback.svelte.ts reference phase/subItem status values
Then they import and use PhaseStatus instead of inline string unions
```

## AC-2: Magic numbers replaced with named constants
```gherkin
Given timing values scattered across the codebase
When referencing SSE heartbeat interval, shutdown grace period, reconnect delay, debounce delay, flash duration, and urgent threshold
Then each uses a named constant with clear intent
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Use PhaseStatus type consistently</name>
  <files>src/lib/types/feedback.ts, src/lib/stores/feedback.svelte.ts</files>
  <action>
    **PA-60: Replace inline string unions with PhaseStatus import**

    In `src/lib/types/feedback.ts`:
    - Add `import type { PhaseStatus } from "../utils/status.js";`
    - Replace `status: "pending" | "approved" | "needs-work"` in phaseStatuses (line 11)
      with `status: PhaseStatus`
    - Replace `status: "pending" | "approved" | "needs-work"` in subItemStatuses (line 20)
      with `status: PhaseStatus`

    In `src/lib/stores/feedback.svelte.ts`:
    - Add `import type { PhaseStatus } from "$lib/utils/status.js";`
    - Replace `status: "pending" | "approved" | "needs-work"` in setPhaseStatus (line 128)
      with `status: PhaseStatus`
    - Replace `status: "pending" | "approved" | "needs-work"` in setSubItemStatus (line 143)
      with `status: PhaseStatus`

    Avoid: Changing the FeedbackPayload.status field ("reviewing" | "approved" | "needs-work")
    — that's a different union with "reviewing" instead of "pending".
  </action>
  <verify>npm run check && npm test</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate magic numbers into named constants</name>
  <files>src/routes/api/sse/[sessionId]/+server.ts, src/routes/api/shutdown/+server.ts, src/lib/stores/plan.svelte.ts, src/lib/stores/feedback.svelte.ts, src/lib/components/feedback/ApprovalBar.svelte, src/lib/components/plan/IdleTimer.svelte</files>
  <action>
    **PA-59: Replace inline magic numbers with named constants**

    Each constant should be defined in the file that uses it (no shared constants file
    for unrelated values). Add a `const` before the first usage:

    1. `src/routes/api/sse/[sessionId]/+server.ts:25` — `30000`
       → `const HEARTBEAT_INTERVAL_MS = 30_000;`

    2. `src/routes/api/shutdown/+server.ts:10` — `500`
       → `const SHUTDOWN_GRACE_MS = 500;`

    3. `src/lib/stores/plan.svelte.ts:58` — `3000`
       → `const RECONNECT_DELAY_MS = 3_000;`

    4. `src/lib/stores/feedback.svelte.ts:31` — `500`
       → `const SAVE_DEBOUNCE_MS = 500;`

    5. `src/lib/components/feedback/ApprovalBar.svelte:28` — `3000`
       → `const FLASH_DURATION_MS = 3_000;`

    6. `src/lib/components/plan/IdleTimer.svelte:19` — `60_000`
       → `const URGENT_THRESHOLD_MS = 60_000;`

    Avoid: Creating a shared constants module — these values are module-specific.
    Avoid: Changing the actual timeout values.
  </action>
  <verify>npm run check && npm test</verify>
  <done>AC-2 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/cli/* (CLI code stable from Phase 7)
- Test files
- Package version

## SCOPE LIMITS
- Type and constant cleanup only — no behavioral changes
- No new dependencies

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm test` — all 160 tests pass
- [ ] `npm run check` — no TypeScript errors
- [ ] `npm run build` — builds successfully
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- PhaseStatus used in all 4 locations
- 6 magic numbers replaced with named constants
- All tests pass, build clean
</success_criteria>

<output>
After completion, create `.paul/phases/08-type-cleanup/08-01-SUMMARY.md`
</output>
